1.0.2
    实现对按键控制、包括（相同或不同的）按键间隔  -----（已完成）

1.1.0
    实现标签属性定义控件类型，节省控件定义：实现方法，定义一个总体area，自动添加子view，如果子view为组控件，内部自己实现，层层递进
        a.通过应用层构建好的结构生成控件      ----结束(放弃)
        b.扫描节点，构成控件id结构         ----结束(放弃)
    常规焦点变化代码省略              ----结束(放弃，算法实现难度较大，页面创建时极易出现死循环)
    将监听方法及属性在标签中设置  -----（已完成）
    设置完之后可以删除在标签上的属性，便于调试阅读   ----结束(放弃，目前的继承结构无法做到删除）
    定义id格式，页面控件布局
    使用 引入document.querySelector(),来查找节点
    自定义页面流程：将initView，setView，initUtil固有方法进行封装  -----（（已完成）

specialView 迭代
    播放器控件 结合player_util开发PlayerView，整合常规操作（上焦之后，上下左右、确定）-----（已完成）

1.2.0
    优化recycleView,包括startIndex获取算法优化，重新渲染子节点算法优化（最大的可能不重新渲染所有子节点，只更新） -----（已完成）
    滚动位置扩展为start、center、end、normal -----（已完成）
    areaView滚动，多层有效，不仅限2层有效 -----（已完成）

1.2.2
    优化scrollView.measure方法，对于只计算一层子节点宽高，计算更深的子节点占位范围    -----（已完成）
    优化areaView中areaChildView的left和top值获取限制，改成基于fatherView(areaView)的坐标（left、top）；实际体验过程中对于刚上手
    时，对于布局局限太大 -----（已完成）
    ListView、GridView、AreaView、RecycleView新增驻留效果功能   -----（已完成）
    初始焦点可以是父控件 -----（已完成）
    单个view可绑定多个跑马灯

specialView 迭代
    新增键盘控件，主要在搜索页使用

虚拟节点，实现列表切换数据无感知

绑定跑马灯标签属性

1.2.6
    ScrollView创建滚动器优化：从innerHTML改为appendDom


1.3.0
    使用相对布局自动生成列多不规则布局子控件，这个不是列表
    支持控件隐藏自动调整位置
    标签属性新增上下左右设置

1.4.0
    优化Dialog控件

1.5.0
    动画选择接口，分无动画、c2动画、c3动画，可判定盒子型号
    dialog显示使用z-index和透明度 属性控制，避免显示、定位及大小获取异常


1.6.0
    实现MVVM结构，省去项目构建步骤

1.7.0
    调研实现懒加载图片，并整合，recycleView实际上就是某种形式的懒加载，实现方式是通过父节点的按需渲染实现的
